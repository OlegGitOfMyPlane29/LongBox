# Токен бота от @BotFather (не коммитить значение в открытый репозиторий)

BOT_TOKEN=



# PostgreSQL: URI подключения (логин, пароль, хост, порт, база).

# Локально: postgresql://user:password@localhost:5432/telebotik

DATABASE_URL=



# Необязательно — если `npm start` не достукивает Telegram (ETIMEDOUT),
# но браузер с VPN открывает сайты: укажите локальный порт из приложения VPN / Clash / v2rayN.
#
# TELEGRAM_PROXY=http://127.0.0.1:7890          # типичный HTTP(S) локальный прокси
# SOCKS_PROXY=socks5://127.0.0.1:10808        # альтернатива SOCKS5
#
# То же действует переменная HTTPS_PROXY (глобально для процесса), если хотите её переиспользовать.




# Необязательно: базовый URL публичного REST Binance (без финального слэша).

# По умолчанию https://api.binance.com — запрос BTC/USDT (спот), без секретов.

# BINANCE_REST_BASE=https://api.binance.com


# --- Утренний дайджест (node-cron). По умолчанию 09:00 локального времени процесса.

# BTC_DIGEST_CRON=0 9 * * *

# Таймзона IANA для CRON (удобно для МСК):

# BTC_DIGEST_TZ=Europe/Moscow


# Только тест: отправка подписчикам каждые N минут (1–59):

# BTC_DIGEST_TICK_MINUTES=10


# Одна рассылка сразу после старта процесса:

# BTC_DIGEST_SMOKE_ON_START=1


